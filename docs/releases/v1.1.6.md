# xer 1.1.6

- Added full CLI integration (`xer-cli`) for opening and managing projects via terminal.
- Added deep link routing (`xer://`) to seamlessly handle CLI commands from outside the app.
- Added automatic, transparent CLI installation on launch.
- Included new UI elements for triggering CLI installation directly from the command bar.
